import 'package:flutter/material.dart';

class TrickArea extends StatelessWidget {
  const TrickArea({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}